# 商品导入工作台发布记录（2026-09-26）

## 发布结果

PR 183 通过全部适用 CI 后 squash 合入 main，功能提交为 `e927ac91ae501da50ed67004b0ee3ed9b618ed0a`。远端功能分支已删除，功能已于 2026-09-26 发布。

本次更新商品导入工作台及所需服务，支持多格式预检、最多三级 SKU、独立草稿复核、任务历史和导出回导。身份与支付功能保持现有行为。

## 验证

commerce lint/test、backend、frontend-contracts、fullstack-smoke 和 preflight-fault-injection 均通过；weapp-smoke 按仓库条件跳过。前端 CI 包含 47 个 mock 场景和 4 个韧性场景。

发布后服务健康、后台静态资源、新接口的匿名访问限制及空结果导出下载均验证通过。没有导入示例商品或修改现有商品数据。

真实试运行母表及原图已在独立测试环境通过解析、事务、并发、重复导入和导出回导验证。完整 1110 行旧表仍待原文件恢复后补验。

## 运维记录

发布与恢复所需的详细环境信息、备份清单和操作回执保留在私有运维记录中，不写入公开文档。后续发布前应由有权限的维护人员核对该记录。
