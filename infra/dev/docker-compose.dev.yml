services:
  identity:
    build:
      context: ../..
      dockerfile: services/identity/Dockerfile.dev
      args:
        GOPROXY: "${DEV_STACK_GOPROXY:-https://goproxy.cn,direct}"
        GOSUMDB: "${DEV_STACK_GOSUMDB:-off}"
        GONOSUMDB: "${DEV_STACK_GONOSUMDB:-*}"
    working_dir: /workspace
    command: >
      sh -c "go run ./services/identity/cmd/identity-migrate && air -c services/identity/.air.toml"
    environment:
      GOPROXY: "${DEV_STACK_GOPROXY:-https://goproxy.cn,direct}"
      GOSUMDB: "${DEV_STACK_GOSUMDB:-off}"
      GONOSUMDB: "${DEV_STACK_GONOSUMDB:-*}"
    volumes:
      - ../..:/workspace
      - tmo-go-mod-cache:/go/pkg/mod
      - tmo-go-build-cache:/root/.cache/go-build

  commerce:
    build:
      context: ../..
      dockerfile: services/commerce/Dockerfile.dev
      args:
        GOPROXY: "${DEV_STACK_GOPROXY:-https://goproxy.cn,direct}"
        GOSUMDB: "${DEV_STACK_GOSUMDB:-off}"
        GONOSUMDB: "${DEV_STACK_GONOSUMDB:-*}"
    working_dir: /workspace
    command: >
      sh -c "go run ./services/commerce/cmd/commerce-migrate && air -c services/commerce/.air.toml"
    environment:
      GOPROXY: "${DEV_STACK_GOPROXY:-https://goproxy.cn,direct}"
      GOSUMDB: "${DEV_STACK_GOSUMDB:-off}"
      GONOSUMDB: "${DEV_STACK_GONOSUMDB:-*}"
    volumes:
      - ../..:/workspace
      - tmo-go-mod-cache:/go/pkg/mod
      - tmo-go-build-cache:/root/.cache/go-build

  payment:
    build:
      context: ../..
      dockerfile: services/payment/Dockerfile.dev
      args:
        GOPROXY: "${DEV_STACK_GOPROXY:-https://goproxy.cn,direct}"
        GOSUMDB: "${DEV_STACK_GOSUMDB:-off}"
        GONOSUMDB: "${DEV_STACK_GONOSUMDB:-*}"
    working_dir: /workspace
    command: >
      sh -c "air -c services/payment/.air.toml"
    environment:
      GOPROXY: "${DEV_STACK_GOPROXY:-https://goproxy.cn,direct}"
      GOSUMDB: "${DEV_STACK_GOSUMDB:-off}"
      GONOSUMDB: "${DEV_STACK_GONOSUMDB:-*}"
    volumes:
      - ../..:/workspace
      - tmo-go-mod-cache:/go/pkg/mod
      - tmo-go-build-cache:/root/.cache/go-build

  ai:
    build:
      context: ../..
      dockerfile: services/ai/Dockerfile.dev
      args:
        GOPROXY: "${DEV_STACK_GOPROXY:-https://goproxy.cn,direct}"
        GOSUMDB: "${DEV_STACK_GOSUMDB:-off}"
        GONOSUMDB: "${DEV_STACK_GONOSUMDB:-*}"
    working_dir: /workspace
    command: >
      sh -c "air -c services/ai/.air.toml"
    environment:
      GOPROXY: "${DEV_STACK_GOPROXY:-https://goproxy.cn,direct}"
      GOSUMDB: "${DEV_STACK_GOSUMDB:-off}"
      GONOSUMDB: "${DEV_STACK_GONOSUMDB:-*}"
    volumes:
      - ../..:/workspace
      - tmo-go-mod-cache:/go/pkg/mod
      - tmo-go-build-cache:/root/.cache/go-build

  gateway-bff:
    build:
      context: ../..
      dockerfile: services/gateway-bff/Dockerfile.dev
      args:
        GOPROXY: "${DEV_STACK_GOPROXY:-https://goproxy.cn,direct}"
        GOSUMDB: "${DEV_STACK_GOSUMDB:-off}"
        GONOSUMDB: "${DEV_STACK_GONOSUMDB:-*}"
    working_dir: /workspace
    command: >
      sh -c "air -c services/gateway-bff/.air.toml"
    environment:
      GOPROXY: "${DEV_STACK_GOPROXY:-https://goproxy.cn,direct}"
      GOSUMDB: "${DEV_STACK_GOSUMDB:-off}"
      GONOSUMDB: "${DEV_STACK_GONOSUMDB:-*}"
    volumes:
      - ./media:/data/media
      - ../..:/workspace
      - tmo-go-mod-cache:/go/pkg/mod
      - tmo-go-build-cache:/root/.cache/go-build

volumes:
  tmo-go-mod-cache:
  tmo-go-build-cache:
